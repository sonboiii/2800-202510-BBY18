<%- include('templates/header', { title: 'Home' }) %>

<% if (typeof user !== 'undefined') { %>
  <h2 class="text-center">Welcome back!</h2>
  <p class="text-center">You're logged in as <strong><%= user.email %></strong>.</p>
<% } else { %>
  <h2 class="text-center">Welcome to Foodie</h2>
  <p class="text-center"><a href="/login">Login</a> or <a href="/signup">Sign Up</a> to get started.</p>
<% } %>

<%- include('templates/footer') %>
